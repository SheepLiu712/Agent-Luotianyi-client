## V0.0.2
修复了一些bug:
1. 修复了历史记录中图片项的处理逻辑，如果图片不存在则向服务器请求重新生成图片。
2. 在返回的历史记录中增加了uuid这一项，通过uuid进行图片项的请求。
3. 服务端将SSL证书换成了受信任的，因此客户端可以正常校验https证书了。
4. 将图片信息的类型名统一为"image"，之前有时会是"picture"，导致客户端无法正确处理图片项。
5. 优化了思考气泡的显示，现在最后一条消息发送完毕之后，不会尝试生成思考气泡再删除。
6. 修复了右键对话时，右键菜单显示样式的问题。